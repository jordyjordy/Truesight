class Backgroundinfo{
    constructor(build) {
        this.notes = build.notes
        this.ideals = build.ideals
        this.bonds = build.bonds
        this.flaws = build.flaws
        this.story = build.story
        this.features = build.features
        this.trinket = build.trinket
    }
}